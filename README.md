# zhengxin's Blog

Built based on [Hugo](https://gohugo.io/) and [Mini](https://themes.gohugo.io/hugo-theme-mini/).

## Development

Init theme submodule by `git submodule update --init --recursive`.

Update theme submodule by `git submodule update --remote --merge`.
